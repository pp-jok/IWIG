interface:
  display_name: "IWIG"
  short_description: "Turn one public XHS note into a traceable local multimodal analysis package."
  default_prompt: "Use IWIG to capture this public Xiaohongshu URL into a local content package and analysis index:"
