# Validation status

Offline schema and unit validation is required for every release. Live video validation only runs with a user-provided `IWIG_VALIDATION_URL`; live image-note validation is `pending_user_test`.
